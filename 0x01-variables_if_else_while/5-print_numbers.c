#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print int number from zero - ten'
 * Return: Always 0 (Suceess)
 */
int main(void)
{
	int p;

	for (p = 0; p < 10; p++)
	printf("%d", p);
	putchar('\n');
	return (0);
}
