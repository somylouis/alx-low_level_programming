#include "main.h"

/**
 * mul - int values to be multiplied
 * @a: first value
 * @b: second value
 * Return: always success
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
